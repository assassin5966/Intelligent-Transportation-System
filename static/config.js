/**
 * 运维页面统一配置（device-info.html / device-config.html / business-rules.html 共用）
 * ---------------------------------------------------------------------------
 * API_BASE: 后端业务服务地址。
 *   - 留空 '' : 使用同源地址（页面由后端 /static 挂载提供，默认即后端本身，推荐）；
 *   - 跨域部署时填写完整地址，如 'http://172.16.168.9:8000'。
 * 页面顶部「后端地址」输入框会预填此值，仍可临时手动修改。
 */
window.OPS_CONFIG = {
  API_BASE: ''
}
